# Syntropia Research RUNBOOK

書籍会社のPhase制とは異なり、本会社は継続運転モデルである。ルーチンは日次/週次/イベント駆動の
3種類で構成する。各ルーチンは対応する `playbooks/*.md` のチェックリストに従って実行する。
本運用ルールは実運転しながら `corp/board.md` の決定ログで固めていく（本ファイルの細部は
随時更新される想定であり、変更は自動push可）。

## 前提

- 全てのルーチンは絶対制約5条（`corp/CLAUDE.md` 第3章）に従う。
- 全ての成果物には front-matter に `as_of`（分析基準日時）を必須記載する。
- リスク上限値の正本は `corp/board.md`（D-002, D-003）。playbook側の数値と食い違う場合は
  board.mdを正とする。
- KPI集計・ダッシュボード生成はCEOが要求した時のみ実施する（自発生成禁止）。

## 日次ルーチン

トリガー: 毎営業日。

1. **スクリーニング**（`playbooks/screening.md`）: 定量フィルタによる一次候補の抽出。
2. **イベント検知**（D-006）: 保有銘柄・WATCH銘柄を対象にEDINET・適時開示情報を巡回する。
   決算発表・適時開示・株式分割・増資等の重大イベントを検知した場合、当日中にイベント駆動
   ルーチンへ引き継ぐ。
3. 保有中ポジション・WATCHリストの株価鮮度チェック（数値実在原則に基づき、取得元・取得日時を
   更新）。
4. 前日までに記録部（`playbooks/logging.md`）へ記録された約定・株価変動から、
   リスク上限（D-002）への抵触がないかの機械照合（`playbooks/risk-check.md`）。

## 週次ルーチン

トリガー: 週1回、**土日**に実施する（D-005）。

1. 日次スクリーニングで積み上がった候補のうち、深掘り対象を企業調査部（`playbooks/research.md`）
   へ送る。
2. 企業調査部の成果物を受けて投資判断部（`playbooks/judgement.md`）が判断メモ（BUY/WATCH/PASS）
   を起案・校閲（1稿＋1校閲）。
3. リスク管理部（`playbooks/risk-check.md`）が全判断メモをD-002/D-003の上限値と機械照合。
   逸脱案はこの時点で提案自体を却下する。
4. 却下されなかった判断メモをCEOへ提示（発注はCEOが手動で行う）。
5. 記録部（`playbooks/logging.md`）が週間サマリ（提案数・機械照合の却下件数など）を記録する。
   ※これはKPIダッシュボードではなく、記録部の定型ログである。

## イベント駆動ルーチン

トリガー: 日次ルーチンのイベント検知（D-006）で決算発表・適時開示・株式分割・増資等の
重大イベントが見つかった場合。

1. 保有銘柄・WATCHリストの対象銘柄でイベントを検知した場合、当日中に企業調査部
   （`playbooks/research.md`）へ緊急調査依頼を発行する。
2. 投資判断部が既存の判断メモを更新するか、新規に起案するかを判断する
   （`playbooks/judgement.md`）。更新の場合も「1稿＋1校閲」を維持する。
3. リスク管理部が更新後の内容を再度機械照合する。
4. CEOへ速報として提示する。

## 記録部のフィードバックループ（唯一の例外）

- 記録部は実際の売買と結果（勝敗・想定シナリオとの乖離）をログする。
- 記録部が「次のスクリーニング条件を変えるべき」と判断した場合でも、エージェントは
  スクリーニング条件を直接書き換えない。
- 教訓案をCEOに提示し、CEO承認後に `corp/board.md` へ新しい決定ID（D-XXX）として記録された
  ものだけを `playbooks/screening.md` に反映する。

## 未確定事項

Phase0時点でのCEOヒアリング項目はD-001〜D-007ですべて確定した。以降の細部（EDINET巡回の
具体的な実装方法、判断メモの文字数目安等）は実運転しながら `corp/board.md` の決定ログで
随時固めていく。

## 既知の技術的制約（未解決・引き継ぎ事項）

初回スクリーニングの試験実行（Phase0完了直後）で発覚した、D-003（Web取得全面許可）の
実行可能性に関する技術的ブロッカー。**新しいセッションを開始した場合は、まずここから
再検証すること。**

### 事象

セッションからの `WebFetch` が、日本の金融データサイトを含む広範な外部サイトに対して
一律 `403 Forbidden` を返す。試行して失敗したURL:

- Yahoo!ファイナンス（`finance.yahoo.co.jp/stocks/ranking/up`）
- 株探（`kabutan.jp`）
- 日本経済新聞（`nikkei.com/marketdata/ranking-jp`）
- TradingView（`jp.tradingview.com`）
- みんかぶ（`minkabu.jp`）
- Google Finance（`google.com/finance`）
- stooq.com（CSV配信）
- EDINET（`disclosure2.edinet-fsa.go.jp`）
- 参考として無関係な一般サイトも失敗: `google.com`、`en.wikipedia.org`、`www.anthropic.com`、
  `jpx.co.jp`、`reuters.com`、`example.com`

一方、以下は成功した:

- `github.com`、`raw.githubusercontent.com`、`gitlab.com`（開発者インフラ系）
- `pypi.org`（プロキシのnoProxyリストに明示的に含まれる）

### 診断

個別サイトのbot対策ではなく、**このセッションが属する環境のネットワークポリシーが
「コーディング用インフラのみ許可」の状態**になっていると判断（`/root/.ccr/README.md`にも
「403/407はプロキシ側の組織ポリシーによるブロックであり、回避せず報告すべき」と明記）。
一般的なWebブラウジング自体がこの環境では許可されていない可能性が高い。

### これまでの対応

1. CEOに環境設定画面（claude.ai/code）でネットワークポリシーを確認・変更してもらうよう依頼
2. CEOが設定変更を実施したと報告（2026-07-09）
3. **同一セッション内で再テストしたが、効果が確認できなかった**（プロキシのポート番号は
   変化していたが、許可範囲は変わらず）
4. 仮説: ネットワークポリシーは環境の起動時に固定され、既存の実行中セッションには反映されず、
   **新しいセッションを開始しないと有効にならない**
5. → 新しいセッションを同じ環境で開始し、再テストする方針で引き継ぎ中

### 次のセッションでやること

1. `WebFetch` で `https://finance.yahoo.co.jp/stocks/ranking/up?market=all` 等を再テストする
2. 成功すれば: `playbooks/screening.md` に従って初回スクリーニングの試験実行を行う
3. 失敗が続く場合: CEOに再報告し、(a) 環境設定の再確認、(b) CEOによる手動データ供給への
   運用切り替え（D-003改定）のいずれかを選んでもらう
4. 解決した場合、このセクションは削除してよい（正常化した制約情報を残す必要はない）
