# Syntropia Research RUNBOOK

書籍会社のPhase制とは異なり、本会社は継続運転モデルである。ルーチンは日次/週次/イベント駆動の
3種類で構成する。各ルーチンは対応する `playbooks/*.md` のチェックリストに従って実行する。
本運用ルールは実運転しながら `corp/board.md` の決定ログで固めていく（本ファイルの細部は
随時更新される想定であり、変更は自動push可）。

## 前提

- 全てのルーチンは絶対制約5条（`corp/CLAUDE.md` 第3章）に従う。
- 全ての成果物には front-matter に `as_of`（分析基準日時）を必須記載する。
- リスク上限値の正本は `corp/board.md`（D-002, D-003）。playbook側の数値と食い違う場合は
  board.mdを正とする。
- KPI集計・ダッシュボード生成はCEOが要求した時のみ実施する（自発生成禁止）。

## 自動化の状態（2026-07-10更新）

Claude Code RemoteのRoutine（スケジュールトリガー）により、以下2本が自動実行される
（`mcp__Claude_Code_Remote__list_triggers`で確認・変更可）:

| ルーチン | トリガーID | スケジュール | 内容 |
|---|---|---|---|
| 日次 | `trig_01JEcWuSFf5uunQSr9gVCxFQ` | 平日 6:30 UTC（15:30 JST、東証引け後） | RUNBOOK.md「日次ルーチン」節に記載の手順（価格照合・シャドーポートフォリオのバリア判定・日次ダイジェスト通知D-020・ダッシュボード再生成） |
| 週次 | `trig_0194icpYLLHsAuEBLh26x8t5` | 土曜 0:00 UTC（9:00 JST） | RUNBOOK.md「週次ルーチン」節に記載の手順（スクリーニング〜投資判断〜リスク管理〜逐次検定D-019・週次サマリD-020） |

**2026-07-10改定**: トリガーのプロンプト自体に手順を列挙する方式（初版）をやめ、
「corp/RUNBOOK.mdの該当節を読んで従うこと」という指示に一本化した（旧トリガーID
`trig_01SMwtDMkudpxvfr2ahWAnYe`・`trig_01C6xrZpqD1qfChTzFN56NZW`は削除・再作成）。
理由: 旧方式ではD-018〜D-020のRUNBOOK/playbook更新がトリガーのプロンプトに反映されず、
発火時に古い手順（逐次検定・日次ダイジェスト等が未反映）で実行される欠陥が見つかったため。
**今後は本ファイルとplaybooks/*.mdを更新すれば、次回発火時から自動的に反映される**
（トリガー自体の再作成は不要）。ただし憲法・数値基準等の実質的な変更を伴う場合は、
引き続きこの表のスケジュール・有効状態を`list_triggers`で確認すること。

いずれもこのセッション（`session_01WMHgc214wewk974tjAAuTD`）に対して発火する設定。
トリガーIDやスケジュールを変更した場合は、この表を更新すること。

## 日次ルーチン

トリガー: 毎営業日（東証の休場日を除く。自動トリガーはcron上「平日」で発火するため、
土日以外の**東証の祝日・年末年始休場**に当たった場合は以下を行う — D-016）:
- 市場が休場であることを確認できた場合、株価取得系の工程（1・2-3・3・5）はスキップし、
  「本日は休場のため価格更新なし」と記録するに留める。
- 休場かどうか不明な場合（祝日カレンダーを持たないため）は、通常通り価格取得を試みてよい
  （取得元が休場を理由に平常と異なる値・古い値を返す場合は、その旨を記録し古い値を最新値と
  誤認しない）。

1. **スクリーニング**（`playbooks/screening.md`）: フロア条件＋発掘条件の2段階方式（D-008）に
   よる一次候補の抽出。
2. **イベント検知**（D-006）: 保有銘柄と、**`corp/watchlist.md`（WATCH銘柄台帳）に記載の
   状態=ACTIVEの銘柄**を対象にEDINET・適時開示情報を巡回する。手順:
   1. `corp/watchlist.md` を読み込み、状態=`ACTIVE` の銘柄と状態=`OPEN` のトリガーを巡回対象とする。
   2. 「予定日」が当日以前のトリガー（決算発表等）を最優先で確認する。
   3. 種別=`決算`/`開示`/`規制` のトリガーは適時開示・IR・ニュースと、種別=`価格` のトリガーは
      当日株価・出来高（取得元・取得日時つき）と照合する。
   4. トリガー成立を検知した場合、当該トリガーの状態を `TRIGGERED` に更新し、当日中に
      イベント駆動ルーチンへ引き継ぐ。
   5. 成立の有無にかかわらず、巡回した銘柄の「最終確認日」を当日に更新する
      （台帳の行は削除しない。運用ルールの詳細は `corp/watchlist.md` 冒頭を参照）。
3. `corp/portfolio.md`（保有ポジション台帳、状態=HELD）・`corp/watchlist.md` 記載銘柄の
   株価鮮度チェック（数値実在原則に基づき、取得元・取得日時を更新。台帳の「時価」列を更新する。
   同時に `corp/data/price-history.json` へ当日の価格ポイントを追記する — D-014）。
4. 前日までに記録部（`playbooks/logging.md`）へ記録された約定・株価変動から、
   リスク上限（D-002・D-011）への抵触がないかの機械照合（`playbooks/risk-check.md`）。
5. **シャドーポートフォリオの価格照合**（D-012、`playbooks/shadow-portfolio.md`）:
   `corp/shadow-portfolio.md` の状態=`OPEN`の仮想ポジションについて当日株価を取得し、
   -10%/+25%バリアへの到達を確認する。到達していれば`HIT_LOSS`/`HIT_PROFIT`に更新し
   決済価格・決済日を記録する。同時に `corp/data/shadow-price-history.json` へ当日の
   価格ポイントを追記する（D-014）。
6. **ダッシュボードの再生成**（開発部DEV、D-014、`playbooks/dev.md` §6-1）: 3・5で
   `corp/data/*.json` が更新された場合、`python3 corp/dev/build_dashboard.py` を実行して
   `corp/dev/dashboard.html` を再生成する（決定論的な定型作業・Haiku可）。CEOが閲覧用の
   常設URL（Artifact）の維持を指示している場合のみ、同一URLへ再デプロイする。
   データ更新が無い日はこの工程をスキップしてよい。
7. **日次ポートフォリオ状況ダイジェストの通知（D-020、必須工程・休場日を除く）**:
   記録部（`playbooks/logging.md`）が保有ポジション数・銘柄別含み損益%・現金残高
   （`corp/cash.md`最新値・申告日つき）・プロベーション期(D-019)該当有無・当日のシャドーPF
   バリア到達件数を1〜3行に要約し、`PushNotification`で送る（詳細手順は
   `playbooks/logging.md`「日次ポートフォリオ状況ダイジェスト」節を参照）。休場と確認できた
   日は送信をスキップする。

## 週次ルーチン

トリガー: 週1回、**土日**に実施する（D-005）。

1. 日次スクリーニングで積み上がった候補のうち、深掘り対象を企業調査部（`playbooks/research.md`）
   へ送る。
2. 企業調査部の成果物を受けて投資判断部（`playbooks/judgement.md`）が判断メモ（BUY/WATCH/PASS）
   を起案・校閲（1稿＋1校閲）。
3. リスク管理部（`playbooks/risk-check.md`）が全判断メモをD-002/D-003/D-011の上限値と機械照合
   （1銘柄上限比率・損切り/利確ライン・セクター集中40%・**同時保有銘柄数上限5**・確信度と
   投入比率の整合、`corp/portfolio.md` 参照）。照合結果は統制語彙 **APPROVE / REJECT** で
   記録し、REJECT（逸脱）案はこの時点で提案自体を却下する（リスク管理部では「PASS」の語を
   使用しない。投資判断部の判定語との衝突回避のため）。
4. APPROVEされた判断メモをCEOへ提示（発注はCEOが手動で行う）。CEOが実際に発注・約定した場合、
   記録部（`playbooks/logging.md`）への報告をもって `corp/portfolio.md` に新規ポジションとして
   追加される（運用ルールは同ファイル冒頭を参照）。
   - **通知（D-016）**: APPROVEされたBUY判断メモが1件以上ある場合、CEOがセッションを
     開いていなくても気づけるよう `PushNotification` で速報する（銘柄コード・判定・
     確信度を1行程度で。詳細はダッシュボード/判断メモを参照させる）。APPROVEがBUYを
     一件も含まない週（全てWATCH/PASS）は通知しない（ノイズ回避）。
5. **WATCH台帳の更新**: 照合完了後、新たにWATCH判定が確定した銘柄と再評価トリガー・
   上振れ余地スコア（高/中/低）を `corp/watchlist.md` に追加し、判定変更（BUY/PASSへの
   格上げ・格下げ）があった銘柄は状態を `CLOSED` に更新する（運用ルールは
   `corp/watchlist.md` 冒頭を参照）。
6. **WATCH台帳の容量チェック（D-010）**: 更新後の状態=ACTIVE銘柄数を数え、20銘柄を超える
   場合は優先度（上振れ余地スコア）が最も低い銘柄から順に、超過分を`CLOSED`化し日次巡回対象
   から除外する（同スコア内はWATCH判定日が古い順）。20銘柄以下ならこの工程はスキップする。
7. **シャドーポートフォリオの新規登録**（D-012）: 今週校閲確定した全ての判断メモ
   （BUY/WATCH/PASSいずれも）を `corp/shadow-portfolio.md` に仮想ポジションとして追加し、
   `corp/data/shadow-price-history.json` にも初期価格ポイントを追記する（運用ルールは
   同ファイル・`playbooks/shadow-portfolio.md` を参照）。
8. 記録部（`playbooks/logging.md`）が週間サマリ（提案数・機械照合の却下件数・シャドー
   ポートフォリオの当週決済件数と勝率・TOPIX(配当込み)対比の超過リターン・LLM運用コスト概算
   など）を記録する（D-012）。※これはKPIダッシュボードではなく、記録部の定型ログである。
   **保存先: `outputs/logging/YYYY-MM-DD-weekly-summary.md`（D-020。YYYY-MM-DDは本ルーチンの
   実行日）。**
   - **逐次検定の機械判定（D-019、必須工程）**: `corp/shadow-portfolio.md` から累計の
     `HIT_PROFIT`/`HIT_LOSS`/`EXPIRED`件数（全区分・BUY区分内訳）を数え、
     `python3 corp/tools/shadow_sprt.py --wins W --losses L --expired E --buy-wins bw --buy-losses bl`
     を実行し、出力（判定: CONTINUE/GRADUATE_RECOMMEND/KILL_TRIGGER/GRAY_STOP）を週間
     サマリへ転記する。**CONTINUE以外の判定が出た場合は`PushNotification`でCEOへ速報する**
     （詳細手順・境界値の根拠は`playbooks/shadow-portfolio.md`参照）。
9. **ダッシュボードの再生成**（開発部DEV、D-014、`playbooks/dev.md` §6-1）: 週内の
   新規ポジション・シャドーポートフォリオ登録・決済が `corp/data/*.json` に反映されて
   いることを確認した上で `python3 corp/dev/build_dashboard.py` を実行し、
   `corp/dev/dashboard.html` を再生成する（Haiku可。常設URL運用中は同一URLへ再デプロイ）。

## イベント駆動ルーチン

トリガー: 日次ルーチンのイベント検知（D-006）で決算発表・適時開示・株式分割・増資等の
重大イベントが見つかった場合。

**本ルーチンの全工程（1〜4）は当日中に完結させる**（D-012）。週次ルーチンのバッチ処理に
合流させない。開示を読む速さはエッジ仮説（D-012）の中核であり、週末まで判断を遅延させると
このルーチンの存在意義が失われる。

1. 保有銘柄・`corp/watchlist.md` 記載の対象銘柄でイベント（トリガー成立）を検知した場合、
   当日中に企業調査部（`playbooks/research.md`）へ緊急調査依頼を発行する。
2. 投資判断部が既存の判断メモを更新するか、新規に起案するかを判断する
   （`playbooks/judgement.md`）。更新の場合も「1稿＋1校閲」を維持する。
3. リスク管理部が更新後の内容を再度機械照合する。
4. CEOへ速報として提示する。**`PushNotification`で通知する（D-016）**——イベント駆動
   ルーチンは定義上「重大イベント」への対応であり、日次ルーチンのBUY通知とは異なり
   判定結果（BUY/WATCH/PASS・APPROVE/REJECT）によらず毎回通知する。

## 記録部のフィードバックループ（唯一の例外）

- 記録部は実際の売買と結果（勝敗・想定シナリオとの乖離）をログする。
- 記録部が「次のスクリーニング条件を変えるべき」と判断した場合でも、エージェントは
  スクリーニング条件を直接書き換えない。
- 教訓案をCEOに提示し、CEO承認後に `corp/board.md` へ新しい決定ID（D-XXX）として記録された
  ものだけを `playbooks/screening.md` に反映する。

## 未確定事項

Phase0時点でのCEOヒアリング項目はD-001〜D-007ですべて確定した。以降の細部（EDINET巡回の
具体的な実装方法、判断メモの文字数目安等）は実運転しながら `corp/board.md` の決定ログで
随時固めていく。

## 既知の技術的制約（診断確定・引き継ぎ事項）

初回スクリーニングの試験実行（Phase0完了直後）で発覚した、D-003（Web取得全面許可）の
実行可能性に関する技術的ブロッカー。**2026-07-09に原因が確定した。ネットワークポリシーの
問題ではなく、`WebFetch` ツール固有のリクエスト（User-Agent等）を一部サイトがボットとして
弾いている、サイト側のbot対策が原因だった。**

### 確定した診断（2026-07-09、別セッションでの検証＋本セッションでのcurl再現）

- `jpx.co.jp`・`kabutan.jp` は `WebFetch` では403だが、**`curl`（プロキシ経由・環境変数変更なし）
  では200 OK**。プロキシの `noProxy` リストにも変化はなく、遮断リストにも入っていない
  （`__agentproxy/status` の `recentRelayFailures` は空）。
- つまり **到達性そのものは最初から問題なかった**。CEOに依頼した環境設定変更（ネットワーク
  ポリシー）は今回の403とは無関係だった可能性が高い。Yahoo!ファイナンス等が新セッションで
  復旧したのは、環境変更のタイミングと偶然重なったか、あるいはサイト側のbot判定が一時的
  だったなど別要因の可能性がある（未検証・結論づけない）。
- **ワークアラウンド**: `jpx.co.jp` / `kabutan.jp` 等、`WebFetch` が403を返すサイトは、
  `Bash` で `curl`（必要ならブラウザ相当の `-A` User-Agent指定）を使い、取得したHTMLを
  読んで内容を確認する方式に切り替えられる。ただし `curl` はHTMLを生で返すため、
  `WebFetch` のようなAI要約は行われない点に注意（自分でパース・要約する）。

### サイト別の現状（2026-07-09時点）

成功（`WebFetch` で到達可）:
- Yahoo!ファイナンス（`finance.yahoo.co.jp`）、日本経済新聞（`nikkei.com`）、
  みんかぶ（`minkabu.jp`）、Google、Wikipedia

`WebFetch` は403だが `curl` なら到達可（bot対策が原因、ワークアラウンド適用可）:
- `jpx.co.jp`（監理・整理銘柄一覧を含む）
- `kabutan.jp`（株探）

解決済み（2026-07-09、EDINET公式APIの調査・実装・検証完了。詳細は本ファイル末尾
「EDINET API連携ツールの整備」節を参照）:
- EDINET（`disclosure2.edinet-fsa.go.jp`）の通常Web UIは引き続きJSレンダリング前のシェルしか
  返らず本文取得不可（この制約自体は解消していない）。**ただし公式のEDINET API
  （`https://api.edinet-fsa.go.jp/api/v2/`）はこのサンドボックスから到達可能であることを
  curlで確認済み（HTTP 200）。以後、EDINETの適時開示・有価証券報告書等の本文取得は
  `python3 corp/tools/edinet_fetch.py`（新規ツール）を正式な手段として使用する。**
  WebFetch/curlでのEDINET直接取得の再試行は不要（このワークアラウンドは廃止）。
  APIキー（Subscription-Key）自体はCEOが取得済みで、実データ取得（NTTの有価証券報告書等）
  にも成功済み。**2026-07-09完全解決**: 環境変数`EDINET_API_KEY`をCEOが環境設定に恒久登録し、
  新しいセッションで実際に反映されていることを確認済み（`edinet_fetch.py --code 9432 --days 60`
  でNTTの実書類6件を取得・APIキーのリダクションも動作確認）。残課題は解消した。

未確定・別問題:
- `finance.yahoo.co.jp/stocks/ranking/value`（売買代金ランキング）→ 400 Bad Request
  （パスを変えても再現、プロキシ・bot対策とは別問題。パス自体が無効な可能性。
  代替として `finance.yahoo.co.jp/stocks/ranking/volume`（出来高ランキング）を使用中）
- 監理・整理銘柄一覧の正しいURLは `https://www.jpx.co.jp/listing/market-alerts/supervision/index.html`
  （旧`supervision-security`パスは誤りで404だった。curlで200確認済み・2026-07-09）

未再テスト: TradingView、Google Finance、stooq.com、reuters.com、example.com、
www.anthropic.com。

### 次にやること

1. `jpx.co.jp`・`kabutan.jp` が必要な場面では `curl`（Bashツール）で取得する運用に切り替える。
   `WebFetch` の403を見たら即座に環境設定変更を依頼するのではなく、まず `curl` で
   到達性を切り分けてから対応方針（ワークアラウンド適用 or 本当のポリシー起因かの報告）
   を決めること。
2. `outputs/screening/2026-07-09-trial.md` で「取得不能」としていた整理銘柄・監理銘柄の
   除外チェックを、上記の正しいURLをcurlで取得して再実施する（フォローアップ済みとして
   マークしてよい）。
3. **【解決済み・2026-07-09】** EDINET APIエンドポイント経由の取得方式の調査・実装・検証が
   完了した（`corp/tools/edinet_fetch.py`。詳細は本ファイル末尾「EDINET API連携ツールの整備」
   節）。CEOが環境変数`EDINET_API_KEY`をclaude.ai/codeの環境設定UIに恒久登録し、新しい
   セッションで反映されていることを確認済み（`edinet_fetch.py --code 9432 --days 60`で
   NTTの有価証券報告書・内部統制報告書・臨時報告書等6件を実データ取得。取得元URLの
   APIキーリダクションも動作確認済み）。日次/週次の自動ルーチンでEDINET本文取得が本格稼働する。
4. 主要データソース（Yahoo!ファイナンス、日経、みんかぶ、curl経由のjpx/kabutan）は
   利用可能なため、`playbooks/screening.md` の運用は継続してよい。

### Yahoo!ファイナンスの一時的な500エラー（2026-07-09、企業調査部RSH-S06で発生）

5803（フジクラ）の企業調査中、`finance.yahoo.co.jp` がWebFetch・curlの両方で終日500エラーを
返し取得不能だった事象を確認。既知の403問題（bot対策）とは別種のエラーで、Yahoo側の一時的な
不調の可能性が高い。代替として株探（kabutan.jp、curl経由）で必要データを取得できたため
作業は継続できたが、**Yahoo!ファイナンスが唯一のデータソースになっている工程では500エラー時の
代替手段（kabutan等への切り替え）をあらかじめ想定しておくこと**。

**再現確認（同日、他5銘柄の調査でも多発）**: `finance.yahoo.co.jp` の個別銘柄ページ・サブページで
9434（ソフトバンク）・4597（ソレイジア・ファーマ）・285A（キオクシアHD）・9984
（ソフトバンクグループ）の調査中にも同様の500エラーが発生（9984では同一URLへの再試行で
200が返るなど間欠的な挙動も観測）。8社中5社で発生しており、単発の偶発障害ではなく
**当日を通じた広範なサービス不調だった可能性が高い**。トップページ・ランキングページは
本日中もWebFetchで取得できていたため、全面障害ではなく個別銘柄ページ系統に偏っている
可能性がある。恒常的な障害かどうかは次回セッションで再確認すること。日経・株探・みんかぶへの
代替で全銘柄とも調査を完了できており、**Yahoo!ファイナンス単独に依存しないマルチソース運用が
有効に機能した**。

### 決算短信PDFのテキスト抽出手法（2026-07-09、企業調査部RSH-S02で確立）

`WebFetch` はPDFバイナリ（決算短信・有価証券報告書等）を直接解析できない。4564
（オンコセラピー・サイエンス）と9434（ソフトバンク）の調査では、以下の手順で一次情報を
直接抽出できた:

1. `curl` で決算短信PDFのURLを取得（会社IRサイトから直接リンクされている場合が多い）
2. `poppler-utils`（`pdftotext`コマンド）で テキストに変換（未インストールの場合は
   `poppler-utils`・`poppler-data` を追加インストールする必要があった）
3. 抽出したテキストから財務数値を読み取り、必要に応じてPythonで再計算（自己資本比率等。
   `corp/CLAUDE.md` 第2章-5「数値の再計算は必ずPythonで行う」に準拠）

一次情報（決算短信・有価証券報告書）に基づく数値は、Yahoo!ファイナンス等の二次情報より
信頼性が高いため、今後の企業調査でも標準手順として採用を検討する価値がある。

### EDINET API連携ツールの整備（2026-07-09、D-012対応・調査/実装/検証済み）

D-012でエッジ仮説（アナリストカバレッジが薄い小型・中型株×適時開示・決算イベント駆動×LLMの
読解量）の中核機能と位置づけられたEDINET本文取得について、公式のEDINET API（通称「書類一覧
API」「書類取得API」）を調査・実装・実際にcurl/Pythonで動作検証した。

**確認できた事実（すべて本セッションで実際にネットワークアクセスして検証済み）**:

- EDINET APIのベースURLは `https://api.edinet-fsa.go.jp/api/v2/` であり、このサンドボックス
  環境から到達可能（`curl`でHTTP 200を確認）。旧v1エンドポイント
  （`disclosure.edinet-fsa.go.jp/api/v1/`）は廃止済みで403を返す。
- v2 APIは「Subscription-Key」（無料のAPIキー）が必須。キーが無い/不正な場合、HTTPステータス
  200のままボディに `{"StatusCode": 401, "message": "Access denied due to invalid
  subscription key...."}` が返る（実際に空キー・ダミーキーの双方で再現確認済み）。これは
  仕様書どおりの正常な認証エラーであり、到達性自体には問題がない。
- APIキーの登録は `https://api.edinet-fsa.go.jp/api/auth/index.aspx?mode=1` から行う
  （実際にcurlでアクセスし、Microsoft Azure AD B2Cのログイン画面へのリダイレクトを確認済み）。
  メールアドレス登録＋確認コード入力、パスワード設定、電話番号によるSMS/自動音声の多要素認証、
  という対話的フローが必須であり、**エージェントのこのセッション内では登録を完結できない
  （メール受信・SMS受信の手段がないため）。CEOご本人の対応が必要。**
  → **2026-07-09追記: CEOがキーを取得済み。** 本セッション内で一時的に環境変数を設定して
  実地検証したところ、NTT（9432）の有価証券報告書・内部統制報告書・臨時報告書等の実データを
  実際に取得できた（下記の実データ取得検証を参照）。
- 一方、証券コード⇔EDINETコードの対応表（「EDINETコードリスト」）は認証不要の公開ZIP
  （`https://disclosure2dl.edinet-fsa.go.jp/searchdocument/codelist/Edinetcode.zip`）として
  配布されており、APIキー無しで取得・利用できることを確認した。本プロジェクトが過去に調査した
  8銘柄（8918, 9432, 9434, 4564, 4597, 5803, 285A, 9984）すべてで証券コード→EDINETコードの
  解決に成功した（例: 9432→E04430＝ＮＴＴ株式会社、285A→E35948＝キオクシアホールディングス
  株式会社）。
- 「書類一覧API」（`GET /documents.json?date=YYYY-MM-DD&type=2&Subscription-Key=...`）・
  「書類取得API」（`GET /documents/{docID}?type=1〜5&Subscription-Key=...`）のリクエスト/
  レスポンス仕様は、EDINET公式のAPI仕様書（Version 2, 2026年6月, 金融庁企画市場局企業開示課、
  `https://disclosure2dl.edinet-fsa.go.jp/guide/static/disclosure/download/ESE140206.pdf`）を
  `curl`でダウンロードし`pdftotext`でテキスト化して仕様を確認した上で実装した（記憶ベースの
  実装ではない）。

**実データ取得検証（2026-07-09、CEOから提供された有効なAPIキーで実地確認済み）**:

- CEOがEDINET APIキーを取得し、本セッション内で一時的に環境変数`EDINET_API_KEY`として設定、
  `search_documents`を実行したところ、NTT（9432）について2026年6月〜7月の実際の提出書類
  （自己株券買付状況報告書・有価証券報告書〔第41期〕・内部統制報告書・確認書・臨時報告書）を
  正しく取得できることを確認した（docID・提出者名・提出日時・概要まで実データで一致）。
  「実装済みだが未検証」だった状態は解消し、**書類一覧APIは実データで検証済み**となった
  （書類取得API＝実ファイルのダウンロードまではまだ実地確認していない）。
- **セキュリティ上の不具合を検知・修正**: 検証中、取得元URLをそのままログ・戻り値に
  格納しており、APIキーが平文で出力に残っていることが判明した。調査メモ等を経由してgit
  管理下のファイルに漏洩する経路になるため、`_redact_url()`関数を追加し、ログ・出典表示・
  例外メッセージ中のAPIキーを`***REDACTED***`に置換するよう修正した（実際のHTTPリクエストは
  影響を受けない）。修正後に再テストし、リダクトされることを確認済み。
- **環境変数の永続化: 解決済み（2026-07-09）**: CEOがclaude.ai/codeの環境設定画面から
  `EDINET_API_KEY`を恒久登録した。新しいセッション（本ルーチンの継続セッション）で
  `python3 corp/tools/edinet_fetch.py --code 9432 --days 60` を実行し、NTTの実書類6件
  （自己株券買付状況報告書・有価証券報告書・内部統制報告書・確認書・臨時報告書）を正しく
  取得できることを再確認した。取得元URLのAPIキーは`***REDACTED***`表示となっており、
  `_redact_url()`も引き続き機能している。

**実装したツール**: `corp/tools/edinet_fetch.py`

- `resolve_edinet_code(sec_code)`: 証券コード→EDINETコードの解決（APIキー不要、動作確認済み）。
- `list_documents_by_date(date)` / `search_documents(...)`: 書類一覧APIによる日付範囲横断検索
  （APIキー必須。エラーハンドリング＝401/404/429/500の判定は仕様書どおりに実装し、ダミーキーで
  401応答の解析まで動作確認済み）。
- `download_document(doc_id, doc_type)`: 書類取得API（XBRL一式/PDF/CSV等のダウンロード。
  APIキー必須、未検証）。
- CLI例: `python3 corp/tools/edinet_fetch.py --code 9432 --days 7`
  （証券コードから直近7日分の書類一覧を検索）、
  `python3 corp/tools/edinet_fetch.py --code 9432 --resolve-only`（APIキー不要、コード解決のみの
  動作確認用）、
  `python3 corp/tools/edinet_fetch.py --doc-id S100XXXX --doc-type 2`（書類PDFのダウンロード）。
- APIキーは環境変数 `EDINET_API_KEY` から読み込む。未設定/無効な場合はCEO向けの登録手順
  （上記と同内容）を含むエラーメッセージを表示して停止する（ハルシネーション防止・第2条準拠）。
- 全出力に「取得元URL」「取得日時」を明示する（第2条準拠）。

**CEOへの依頼事項（2026-07-09時点ですべて完了）**:

1. ~~APIキーの取得~~ **完了済み**（CEOがAPIキーを取得し、実データ取得まで確認済み）。
2. ~~環境変数`EDINET_API_KEY`の恒久登録~~ **完了済み**（2026-07-09、CEOがclaude.ai/codeの
   環境設定画面から登録。新しいセッションで反映・実データ取得を再確認済み）。
3. 日次/週次の自動ルーチンでEDINET本文取得が本格稼働する状態になった。書類取得API
   （実ファイルのダウンロード）自体の実地確認は、今後の企業調査部の実運用の中で行う
   （書類一覧APIは実データ検証済み、書類取得APIはコード実装済みだが未実地確認のまま）。
